create table nodes (	
	index_key     varchar(32),
	monitor_ip    varchar(15),
	monitor_port  numeric,  
	alias         varchar(20),
	inserted_times varchar(14),
);


create table node_info (
	index_key   varchar(32),
);

create table node_system_info (
	index_key	      varchar(32),
	cpu_num           numeric,
	cpu_process_info  varchar(32),
    opsys_info        vachar(32),
	mm_num 		      numeric,
	mm_size			  varchar(12),
	mm_free_size      varchar(12),
	hd_size           varchar(32),
	hd_num			  numeric,
	hd_free_size    numeric,
	hd_no             varchar(10),
	hd_no_size        numeric,
	hd_free_size      numeric,
);

create table node_hd_info (
	index_key	      varchar(32),
	hd_no             varchar(10),
	hd_no_size        numeric,
	hd_free_size      numeric,
	hd_in
);;
